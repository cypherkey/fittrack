# FitTrack frontend

Angular 21 SPA (NgModules + Angular Material). See [`../docs/FRONTEND.md`](../docs/FRONTEND.md).

```bash
npm install
npm start
```

Dev server: http://localhost:4200 (expects API at http://localhost:8080).
