spring:
  application:
    name: fittrack-backend
  datasource:
    url: jdbc:sqlite:${FITTRACK_DB_PATH:./data/fittrack.db}?foreign_keys=true&journal_mode=WAL
    driver-class-name: org.sqlite.JDBC
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
    properties:
      hibernate:
        dialect: org.hibernate.community.dialect.SQLiteDialect
        type:
          preferred_instant_jdbc_type: VARCHAR
          preferred_boolean_jdbc_type: INTEGER
          preferred_uuid_jdbc_type: CHAR
  flyway:
    enabled: true
    locations: classpath:db/migration
  security:
    oauth2:
      client:
        registration:
          google:
            client-id: ${GOOGLE_CLIENT_ID:}
            client-secret: ${GOOGLE_CLIENT_SECRET:}
            scope: openid, profile, email

server:
  port: ${SERVER_PORT:8080}

management:
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      probes:
        enabled: true
      show-details: never

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
    operations-sorter: method
    tags-sorter: alpha
  show-actuator: false

fittrack:
  jwt:
    secret: ${JWT_SECRET:fittrack-dev-secret-change-me-must-be-at-least-256-bits-long!!}
    expiration-minutes: ${JWT_EXPIRATION_MINUTES:720}
  seed:
    default-username: ${FITTRACK_DEFAULT_USER:admin}
    default-password: ${FITTRACK_DEFAULT_PASSWORD:admin}
    default-display-name: ${FITTRACK_DEFAULT_DISPLAY_NAME:Admin}
    default-email: ${FITTRACK_DEFAULT_EMAIL:admin@localhost}
    # Load image bytes into image.content_base64 (classpath data/exercise-images/, else GitHub raw)
    load-images: ${FITTRACK_SEED_LOAD_IMAGES:true}
    download-images: ${FITTRACK_SEED_DOWNLOAD_IMAGES:true}
  oauth2:
    google:
      enabled: ${FITTRACK_GOOGLE_OAUTH_ENABLED:false}
    success-redirect: ${FITTRACK_SPA_AUTH_CALLBACK_URL:http://localhost:4200/auth/callback}
  cors:
    allowed-origins: ${FITTRACK_CORS_ORIGINS:http://localhost:4200}

logging:
  level:
    org.springframework.security: INFO
